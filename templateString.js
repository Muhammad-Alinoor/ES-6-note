// multiline string is declared inside of a carret=backtick=backquote sign.
const first = "labib";
const second = "Chowdhury";
// now will declare variable within second bracket in backtick
const fullNew = `${first} ${second}`;
console.log(fullNew);
console.log(`${fullNew}`);